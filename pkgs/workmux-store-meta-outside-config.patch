diff --git a/src/git/branch.rs b/src/git/branch.rs
index d9b18bdf..19d5b751 100644
--- a/src/git/branch.rs
+++ b/src/git/branch.rs
@@ -381,7 +381,9 @@ pub fn set_branch_base(branch: &str, base: &str) -> Result<()> {
 /// Store the base branch/commit in a specific workdir
 pub fn set_branch_base_in(branch: &str, base: &str, workdir: Option<&Path>) -> Result<()> {
     let config_key = format!("branch.{}.workmux-base", branch);
-    let cmd = Cmd::new("git").args(&["config", "--local", &config_key, base]);
+    let cfg = super::repo::get_workmux_config_path_in(workdir)?;
+    let cfg_s = cfg.to_string_lossy().into_owned();
+    let cmd = Cmd::new("git").args(&["config", "--file", &cfg_s, &config_key, base]);
     let cmd = match workdir {
         Some(path) => cmd.workdir(path),
         None => cmd,
@@ -398,7 +400,9 @@ pub fn get_branch_base(branch: &str) -> Result<String> {
 /// Get the base branch for a given branch in a specific workdir
 pub fn get_branch_base_in(branch: &str, workdir: Option<&Path>) -> Result<String> {
     let config_key = format!("branch.{}.workmux-base", branch);
-    let cmd = Cmd::new("git").args(&["config", "--local", &config_key]);
+    let cfg = super::repo::get_workmux_config_path_in(workdir)?;
+    let cfg_s = cfg.to_string_lossy().into_owned();
+    let cmd = Cmd::new("git").args(&["config", "--file", &cfg_s, &config_key]);
     let cmd = match workdir {
         Some(path) => cmd.workdir(path),
         None => cmd,
diff --git a/src/git/repo.rs b/src/git/repo.rs
index 77801064..0e0ba01d 100644
--- a/src/git/repo.rs
+++ b/src/git/repo.rs
@@ -137,6 +137,16 @@ pub fn get_git_common_dir() -> Result<PathBuf> {
     get_git_common_dir_in(None)
 }
 
+/// Path to workmux's metadata file alongside `.git/config`.
+///
+/// workmux stores `branch.<n>.workmux-base` and `workmux.worktree.<n>.<k>` in
+/// `$GIT_COMMON_DIR/workmux-config` rather than `.git/config` so sandboxes
+/// that deny writes to `.git/config` (Claude Code's built-in sandbox among
+/// them) don't block `workmux add`.
+pub fn get_workmux_config_path_in(workdir: Option<&Path>) -> Result<PathBuf> {
+    Ok(get_git_common_dir_in(workdir)?.join("workmux-config"))
+}
+
 /// Get the common git directory for a repository at a specific path.
 pub fn get_git_common_dir_in(workdir: Option<&Path>) -> Result<PathBuf> {
     let cmd = Cmd::new("git").args(&["rev-parse", "--git-common-dir"]);
diff --git a/src/git/worktree.rs b/src/git/worktree.rs
index f5f67278..2a9bc42c 100644
--- a/src/git/worktree.rs
+++ b/src/git/worktree.rs
@@ -110,8 +110,10 @@ pub fn migrate_worktree_meta(old_handle: &str, new_handle: &str) -> Result<()> {
     }
     let old_section = format!("workmux.worktree.{}", old_handle);
     let regex_pattern = format!(r"^{}\.", regex::escape(&old_section));
+    let cfg = super::repo::get_workmux_config_path_in(None)?;
+    let cfg_s = cfg.to_string_lossy().into_owned();
     let output = Cmd::new("git")
-        .args(&["config", "--local", "--get-regexp", &regex_pattern])
+        .args(&["config", "--file", &cfg_s, "--get-regexp", &regex_pattern])
         .run_and_capture_stdout()
         .unwrap_or_default();
 
@@ -126,14 +128,14 @@ pub fn migrate_worktree_meta(old_handle: &str, new_handle: &str) -> Result<()> {
         };
         let new_key = format!("workmux.worktree.{}.{}", new_handle, suffix);
         Cmd::new("git")
-            .args(&["config", "--local", &new_key, value])
+            .args(&["config", "--file", &cfg_s, &new_key, value])
             .run()
             .with_context(|| format!("Failed to set {}", new_key))?;
     }
 
     // Remove the old section (ignore "no such section" errors).
     let _ = Cmd::new("git")
-        .args(&["config", "--local", "--remove-section", &old_section])
+        .args(&["config", "--file", &cfg_s, "--remove-section", &old_section])
         .run();
 
     Ok(())
@@ -319,7 +321,9 @@ pub fn set_worktree_meta_in(
     workdir: Option<&Path>,
 ) -> Result<()> {
     let config_key = format!("workmux.worktree.{}.{}", handle, key);
-    let cmd = Cmd::new("git").args(&["config", "--local", &config_key, value]);
+    let cfg = super::repo::get_workmux_config_path_in(workdir)?;
+    let cfg_s = cfg.to_string_lossy().into_owned();
+    let cmd = Cmd::new("git").args(&["config", "--file", &cfg_s, &config_key, value]);
     let cmd = match workdir {
         Some(path) => cmd.workdir(path),
         None => cmd,
@@ -339,7 +343,9 @@ pub fn get_worktree_meta(handle: &str, key: &str) -> Option<String> {
 /// Retrieve per-worktree metadata from git config in a specific workdir.
 pub fn get_worktree_meta_in(handle: &str, key: &str, workdir: Option<&Path>) -> Option<String> {
     let config_key = format!("workmux.worktree.{}.{}", handle, key);
-    let cmd = Cmd::new("git").args(&["config", "--local", "--get", &config_key]);
+    let cfg = super::repo::get_workmux_config_path_in(workdir).ok()?;
+    let cfg_s = cfg.to_string_lossy().into_owned();
+    let cmd = Cmd::new("git").args(&["config", "--file", &cfg_s, "--get", &config_key]);
     let cmd = match workdir {
         Some(path) => cmd.workdir(path),
         None => cmd,
@@ -417,7 +423,11 @@ pub fn get_all_worktree_meta_key_in(
     key_name: &str,
 ) -> std::collections::HashMap<String, String> {
     let pattern = format!(r"^workmux\.worktree\..*\.{}$", regex::escape(key_name));
-    let cmd = Cmd::new("git").args(&["config", "--local", "--get-regexp", &pattern]);
+    let Ok(cfg) = super::repo::get_workmux_config_path_in(workdir) else {
+        return std::collections::HashMap::new();
+    };
+    let cfg_s = cfg.to_string_lossy().into_owned();
+    let cmd = Cmd::new("git").args(&["config", "--file", &cfg_s, "--get-regexp", &pattern]);
     let cmd = match workdir {
         Some(path) => cmd.workdir(path),
         None => cmd,
@@ -445,9 +455,14 @@ pub fn get_all_worktree_meta_key_in(
 pub fn get_all_worktree_modes_in(
     workdir: Option<&Path>,
 ) -> std::collections::HashMap<String, MuxMode> {
+    let Ok(cfg) = super::repo::get_workmux_config_path_in(workdir) else {
+        return std::collections::HashMap::new();
+    };
+    let cfg_s = cfg.to_string_lossy().into_owned();
     let cmd = Cmd::new("git").args(&[
         "config",
-        "--local",
+        "--file",
+        &cfg_s,
         "--get-regexp",
         r"^workmux\.worktree\..*\.mode$",
     ]);
@@ -484,11 +499,16 @@ pub fn get_all_worktree_modes_in(
 pub fn remove_worktree_meta_at(handle: &str, git_common_dir: &Path) -> Result<()> {
     let section = format!("workmux.worktree.{handle}");
     let key_pattern = format!(r"^{}\.", regex::escape(&section));
+    let cfg_s = git_common_dir
+        .join("workmux-config")
+        .to_string_lossy()
+        .into_owned();
     let mut probe = super::unattended_git(Some(git_common_dir))?;
     let probe_output = probe
         .args([
             "config",
-            "--local",
+            "--file",
+            cfg_s.as_str(),
             "--name-only",
             "--get-regexp",
             &key_pattern,
@@ -507,7 +527,7 @@ pub fn remove_worktree_meta_at(handle: &str, git_common_dir: &Path) -> Result<()
 
     let mut remove = super::unattended_git(Some(git_common_dir))?;
     let remove_output = remove
-        .args(["config", "--local", "--remove-section", &section])
+        .args(["config", "--file", cfg_s.as_str(), "--remove-section", &section])
         .output()
         .context("Failed to remove Workmux metadata")?;
     if !remove_output.status.success() {
