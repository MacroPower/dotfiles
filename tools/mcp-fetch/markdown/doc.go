// Package markdown converts an HTML document to Markdown. It first runs
// readability extraction to isolate the main article and falls back to
// converting the whole document when extraction yields nothing usable.
package markdown
