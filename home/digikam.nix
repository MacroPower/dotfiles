# digiKam config lives at ~/Library/Preferences/digikamrc on macOS
# (Qt QStandardPaths under the KDE Homebrew cask). On Linux it is
# ~/.config/digikamrc.
#
# Intentionally empty: do NOT declare `home.file` for digikamrc until
# digiKam has run once and a collection root and database backend
# have been chosen. A pre-existing rc file suppresses the first-run
# wizard (KDE Bug 412650). Future declarative settings (collection
# paths, face-recognition thresholds, etc.) belong here.
_:

{ }
