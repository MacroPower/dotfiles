The cache refills every second, ensuring steady throughput.
The parser skips fenced blocks, allowing code to pass untouched.
Each rule lives in its own file, making review easy.
The report lists every finding, highlighting the worst first.
The server returns 503, indicating overload.
The retry budget is small, resulting in fast failure.
The rule stays blunt, making the fix mechanical.
The wrapper skips YAML, preventing string literals from tripping the linter.
The bucket never refills, meaning the burst is all you get.
The lock is held for the whole sweep, leading to a stall.
The wrapper reads the environment, providing a fallback for each key.
