The parser supports tables.
The client accepts a timeout.
The flag defaults to five.
The tool runs without prompting.
Set returns the new count.
Add returns the new length of the list.
The entry is no longer valid after Close.
