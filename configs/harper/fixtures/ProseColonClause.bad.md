A rate of zero is the mirror image: the bucket never refills.
The check is a heuristic: it trips on short pages too.
The parser is strict: a stray tab fails the whole file.
This is the tell: an em dash would fit the same slot.
v1.0 is out: it ships a thread-safe token bucket.
The check is cheap: it reads one byte.
The lock is held briefly: the map update is a single assignment.
There is one exception: you can pass a file path.
Robots handling fails open: any fetch error is allowed.
The retry is bounded: the client will give up after five attempts.
Rule order matters: the first match wins.
We chose Lix for one reason: it builds faster.
The answer is simple: the cache was never warm.
Set the env var: it controls the port.
